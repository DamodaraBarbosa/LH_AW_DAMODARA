version: 2

models:
  - name: stg_countryregion
    description: This staging contains info about the country regions
    columns:
      - name: countryregioncode
        description: Standard code for countries and regions
        tests:
          - not_null
          - unique
      - name: name
        description: Country's names
        tests:
          - not_null
          - unique